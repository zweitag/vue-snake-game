export enum Direction {
  RIGHT = "RIGHT",
  LEFT = "LEFT",
  UP = "UP",
  DOWN = "DOWN",
}

export enum Status {
  MOVED = 'MOVED',
  ATE = 'ATE',
  COLLIDED = 'COLLIDED',
  INITIALIZED = 'INITIALIZED',
}
